# Prompt Productivity — istruzioni per gli agenti

## Prima di lavorare

- Leggi `README.md` e `docs/STATUS.md`.
- Spiega ad Andrea in italiano semplice cosa farai e perché.
- Tratta `C:\AgenticWorkspace\PROGETTI\LAVORO\AI_PER_ARCHITETTI\workshop-ai-per-architette\risorse\prompt-productivity` come radice canonica locale.
- Considera questo repository una risorsa del progetto `workshop-ai-per-architette`, non un progetto autonomo sul piano organizzativo.
- La copia OneDrive è legacy: non modificarla, sincronizzarla o eliminarla automaticamente.

## Mappa del progetto

- `index.html`: contenuto e struttura del mini-sito.
- `style.css`: presentazione e comportamento responsivo.
- `script.js`: ricerca, copia negli appunti e finestra del codice QR.
- `logo-al.svg`, `qrcode.png`: asset pubblici necessari.
- `docs/STATUS.md`: unica fonte per attività aperte e completate.

## Comandi verificati

- Controllo JavaScript: `node --check script.js`.
- Stato Git: `git status --short --branch`.

Non inventare una fase di build: il progetto non usa attualmente dipendenze o compilazione.

## Regole di lavoro

- Mantieni la radice semplice; non aggiungere framework o dipendenze senza un beneficio concreto.
- Non inserire segreti, dati di partecipanti o materiali privati nel repository pubblico.
- Verifica su telefono e desktop le modifiche visive rilevanti.
- Ricorda che un push sul ramo `main` può aggiornare il sito pubblico: controlla prima il diff e il flusso reale.
- Registra qui solo lo stato di questo mini-sito; le attività trasversali appartengono al wrapper del workshop.

## Definizione di completato

Una modifica è completa quando sintassi e file richiesti sono validi, il flusso interessato è stato provato nel browser, il repository è pulito e `docs/STATUS.md` riporta una prova sintetica.
